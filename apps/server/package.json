{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server"
  },
  "dependencies": {
    "@joyfuljuices/auth": "workspace:*",
    "@joyfuljuices/db": "workspace:*",
    "@joyfuljuices/env": "workspace:*",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.8.2",
    "stripe": "^22.0.2",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "@joyfuljuices/config": "workspace:*",
    "@types/node": "^22.13.14",
    "alchemy": "catalog:",
    "tsdown": "^0.16.5",
    "typescript": "^5",
    "wrangler": "catalog:"
  }
}
